{
  "name": "@mmid/main-app",
  "version": "1.0.0",
  "main": "dist/index",
  "types": "dist/index",
  "files": [
    "dist"
  ],
  "license": "MIT",
  "scripts": {
    "build": "yarn run clean && yarn run compile",
    "clean": "rm -rf ./dist",
    "compile": "tsc -p tsconfig.build.json"
  },
  "dependencies": {
    "@mmid/components": "1.0.0"
  },
  "peerDependencies": {
    "react": "*",
    "react-dom": "*"
  }
}
